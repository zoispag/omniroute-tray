{
  "name": "omniroute-tray",
  "version": "0.1.2",
  "description": "A cross-platform tray app that supervises, monitors, and auto-updates the OmniRoute AI router.",
  "license": "MIT",
  "type": "module",
  "private": true,
  "scripts": {
    "dev": "vite",
    "build": "vite build",
    "preview": "vite preview",
    "tauri": "tauri"
  },
  "devDependencies": {
    "@tauri-apps/cli": "^2.11.0",
    "vite": "^8.1.3"
  },
  "dependencies": {
    "@tauri-apps/api": "^2.0.0"
  }
}
